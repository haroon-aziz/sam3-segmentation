# Core wrapper dependencies
opencv-python>=4.8.0
numpy>=1.24.0

# SAM 3 itself is not on PyPI - install from source (see README):
#   git clone https://github.com/facebookresearch/sam3.git
#   cd sam3 && pip install -e .
